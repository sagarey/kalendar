# 使用 Ubuntu 基础镜像（如果可用）
FROM ubuntu:latest

# 安装 Python
RUN apt-get update && apt-get install -y python3 && rm -rf /var/lib/apt/lists/*

# 设置工作目录
WORKDIR /app

# 复制 Python 脚本
COPY hello.py .

# 运行应用程序
CMD ["python3", "hello.py"]